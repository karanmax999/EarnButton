'use client'

import { AgentDashboard } from '@/components/agent'

/**
 * Agent Dashboard Page
 * 
 * Main page for the agent dashboard feature at /agent route.
 * Renders the AgentDashboard component which orchestrates all
 * agent-related functionality.
 */
export default function AgentPage() {
  return <AgentDashboard />
}
